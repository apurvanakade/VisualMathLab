/.quarto/
**/*.quarto_ipynb
.DS_Store
docs/
node_modules/

# Generated by `npm run build:mathviz` (scripts/sync-mathviz.mjs). The build
# writes these two intermediates on its way to _extensions/mathviz/, which is
# the only copy this repo commits; the mathviz mirror regenerates its own.
_mathviz/dist/
_mathviz/_extensions/mathviz/dist/
